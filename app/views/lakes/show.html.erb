
<div class="row">
 <aside>
<%= @lake.lake_image %>
<h3><%= @lake.name_of_lake %></h3>
<h3><%= @lake.content %></h3>
<h3><%= @lake.lake_zipcode %></h3>
    </aside>
     <div class="span8">
    <% if @user.microposts.any? %>
      <h3>Microposts (<%= @user.microposts.count %>)</h3>
      <ol class="microposts">
        <%= render @microposts %>
      </ol>
      <%= will_paginate @microposts %>
    <% end %>
  </div>
  </div>
  